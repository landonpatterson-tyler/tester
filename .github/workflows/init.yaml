name: test
on:
  push:
    branches: ["master", "main", "dev", "uat"]
    paths-ignore:
    - .github/workflows/**
permissions:
  contents: read
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3